#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 * @t: compute the absolute value of an integer
 * Return: The absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
